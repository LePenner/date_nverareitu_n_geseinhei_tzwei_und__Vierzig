class Ticket():
    def __init__(self, question = str, thread_id = str, history = dict, product_tags = list, problem_tags = list, level = int, extra_bin = str):
        super().__init__
        self.question = question
        self.history = history
        self.thread_id = thread_id
        self.product_tags = product_tags
        self.problem_tags = problem_tags
        self.level = level
        self.extra_bin = extra_bin

    def escalate():
        pass
    
    def close():
        pass